# 🚢 International Shipping Service (AI Implementation Guide)

This document provides the necessary technical details and code snippets to implement or update the **International Shipping from China** service using AI.

## 1. Service Metadata

| Field          | Value                                                                                          |
| :------------- | :--------------------------------------------------------------------------------------------- |
| **Name (EN)**  | International Shipping from China (Air & Sea)                                                  |
| **Name (AR)**  | خدمة الشحن الدولي من الصين (جوي وبحري)                                                         |
| **Category**   | Logistics / الشحن واللوجستيات                                                                  |
| **Base Price** | $50.00 (5000 cents)                                                                            |
| **Image URL**  | `https://images.unsplash.com/photo-1494412519320-aa613dfb7738?auto=format&fit=crop&q=80&w=800` |

## 2. Database Schema (Drizzle ORM)

The service is stored in the `services` table. Use the following structure for insertion:

```typescript
// Path: drizzle/schema.ts
export const services = mysqlTable("services", {
  id: int("id").autoincrement().primaryKey(),
  factoryId: int("factoryId").notNull(), // Use 999 for IFROF Logistics
  name: text("name").notNull(),
  description: text("description"),
  category: varchar("category", { length: 100 }),
  basePrice: int("basePrice").notNull(),
  imageUrls: text("imageUrls"), // JSON array string
  active: int("active").default(1),
});
```

## 3. Backend Implementation (tRPC)

Ensure the `servicesRouter` is correctly fetching data:

```typescript
// Path: server/routers/services.ts
export const servicesRouter = router({
  list: publicProcedure.query(async ({ ctx }) => {
    return await ctx.db
      .select()
      .from(schema.services)
      .where(eq(schema.services.active, 1));
  }),
});
```

## 4. Seeding Script (Automation)

To add this service to any environment (Local/Railway), run:

```bash
npx tsx scripts/seed-shipping-service.ts
```

## 5. Frontend Integration

The service will automatically appear on the `/services` page once the database is seeded. To customize the display, edit:

- **Page:** `client/src/pages/Services.tsx`
- **Component:** `Card` rendering logic for `Logistics` category.

---

_Generated by Manus DevOps for ifrof.com_

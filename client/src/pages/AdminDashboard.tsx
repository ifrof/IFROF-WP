
---

## 2) `AdminDashboard.tsx` (clean)

> You had it duplicated twice — keep **only one** file like this.

```tsx
import AdminApp from "@/admin/AdminApp";

export default function AdminDashboard() {
  return <AdminApp />;
}

# Railway Environment Variables Template
# Copy these to Railway Dashboard → Service → Variables

# ============================================
# REQUIRED VARIABLES
# ============================================

# Node Environment
NODE_ENV=production

# Database (Railway MySQL - use variable reference)
DATABASE_URL=${{MySQL.DATABASE_URL}}

# OAuth Configuration (Manus Auth)
VITE_OAUTH_PORTAL_URL=https://auth.manus.im
VITE_APP_ID=ifrof-prod
OAUTH_SERVER_URL=https://auth.manus.im

# Security Keys (GENERATE THESE!)
# Run: openssl rand -hex 32
ENCRYPTION_KEY=REPLACE_WITH_GENERATED_KEY

# Run: openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_GENERATED_KEY

# Application URLs (Update with your domain)
FRONTEND_URL=https://ifrof.com
BACKEND_URL=https://ifrof.com

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Feature Flags
ENABLE_EMAIL_NOTIFICATIONS=true
ENABLE_STRIPE_PAYMENTS=true

# ============================================
# OPTIONAL VARIABLES (Add if needed)
# ============================================

# Stripe Configuration (Uncomment if using payments)
# STRIPE_SECRET_KEY=sk_live_your_secret_key_here
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
# STRIPE_PUBLIC_KEY=pk_live_your_public_key_here

# Email Configuration (Uncomment if using email)
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-specific-password
# EMAIL_FROM=noreply@ifrof.com

# ============================================
# NOTES
# ============================================
# 1. Generate security keys using:
#    openssl rand -hex 32 (for ENCRYPTION_KEY)
#    openssl rand -base64 32 (for JWT_SECRET)
#
# 2. DATABASE_URL will be automatically set by Railway MySQL service
#    Use the variable reference: ${{MySQL.DATABASE_URL}}
#
# 3. Update FRONTEND_URL and BACKEND_URL with your actual domain
#
# 4. For Stripe, use production keys (sk_live_*, pk_live_*)
#
# 5. For Gmail, use App-Specific Password (not your regular password)
#    Generate at: https://myaccount.google.com/apppasswords
